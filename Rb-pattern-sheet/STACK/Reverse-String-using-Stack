// User function Template for C++
class Solution {
  public:
    string reverseString(string& s) {
        // code here
        stack<char> st;
        int i;
        
        for(int i=0;i<n;i++){
            st.push(s[i]);
        }
        
        while(!st.empty())
        {
            char c=st.top();
            st.pop();
            res.push_back(c);
        }
        return res;
    }
};
