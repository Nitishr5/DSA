#include <iostream>
using namespace std;

int fact(int n)
{
    //Base case
    if(n==1||n==0)  // n=0 for when user give in input 0 then factorial of 0 is 1
    return 1;
    
   return n*fact(n-1);
}
int main() {
    // Write C++ code here
   int n;
   n=5;
  cout<<fact(n);
}
