// Online C++ compiler to run C++ program online
#include <iostream>
#include<queue>
using namespace std;
int main() {
    // Write C++ code here
  queue<int> q;
  
  q.push(2);
  q.push(3);
  q.push(4);
  q.push(5);
  
  cout<<q.size()<<endl;
  q.pop();
  
  cout<<q.size();
  
  if(q.empty()){
      cout<<"Queue is empty"<<endl;
  }
  else{
      cout<<"Queue is Not empty"<<endl;
  }
}
